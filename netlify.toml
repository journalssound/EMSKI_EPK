[build]
  publish = "dist"
  command = "npm run build"

# Route emski-music.com to the /emski-music subfolder
[[redirects]]
  from = "/*"
  to = "/emski-music/:splat"
  status = 200
  conditions = { Host = ["emski-music.com", "www.emski-music.com"] }

# Default: serve the main EPK site for emski-epk.com
# (all other traffic goes to the normal build output)
